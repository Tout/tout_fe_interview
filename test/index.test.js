const expect = require('expect.js');
const today = require('../src/index.js');

describe('Placeholder Test', () => {
  it('is just a placeholder for real tests', () => {
    expect(today).to.be.a('number');
  });
});
